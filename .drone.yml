pipeline:
 mytask:
  image: 
   repo:
	- lastone/Dockerfile
 
  script:
   - docker build -t newist lastone/Dockerfile
   - docker ps -a
   - docker login -u reoltech -p Reolus@123
   - docker image tag newist reoltech/ns:1
   - docker image push reoltech/ns:1
   - echo "done"
  when:
    event: push
